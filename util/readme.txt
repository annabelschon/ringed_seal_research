An a.out file can be run with this command, to produce .err
and .list files, where the .err file lists the run time in
human readable format:

run_a.out

run_a.out calls run_a.out.script.  Then an email is sent to
the user when the run is finished.

